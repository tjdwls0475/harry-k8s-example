apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    app: nginx
spec:
  containers:
  - image: nginx:1.14
    name: nginx
    ports:
    - containerPort: 80
    volumeMounts:
    - name: nfs
      mountPath: "/"
  volumes:
  - name: nfs
    persistentVolumeClaim:
      claimName: nfs
